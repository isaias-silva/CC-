#include <stdio.h>
#include <stdlib.h>
#include <iostream>

using namespace std;

// criar struct no
typedef struct no No;
struct no
{
    int num;// valor
    struct no *proximo;//ponteiro da struct no
};

int main()
{
    

    return 0;
}